// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("ocqprint", "for ocqprint support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("ocqprint", "enable ocqprint support", "no");

if (PHP_OCQPRINT != "no") {
	EXTENSION("ocqprint", "ocqprint.c");
}

